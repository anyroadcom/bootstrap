/*
 * In order to register all the jquery helpers
 * we need to require them.
 * They don't export anything because they attach to jquery.
 * So we don't export anything either.
 */

require('./js/tooltips.js');
